/**
 * Base shared flat ESLint config for the Tempo monorepo.
 *
 * Framework-specific presets live alongside this entrypoint
 * (e.g. `@tempo/eslint-config/next`). Kept intentionally minimal in Phase 0;
 * shared base rules for non-framework TS packages land here as the codebase grows.
 */
export default [];
